# Code Review Guidelines

## 1. Anti-Patterns
> 아래 항목 중 하나라도 해당된다면 절대 머지하지 않으며 **[Critical]** 또는 **[Warning]** 으로 지적한다.
- API Key/Secret/DB Password 등 보안상 중요한 정보가 소스코드나 설정 파일에 평문으로 노출됨 
- 특정 사용자의 데이터 조회 시 쿼리에 사용자 필터링 조건이 없음

## 2. Code Review Check List
### 2.1. 예외 처리
- 같은 도메인의 `ErrorCode`에 이미 존재하는 원인에 대한 코드가 중복으로 정의되지 않았는가?
- 비즈니스 예외 클래스가 `BusinessException`을 상속하고, 부모의 생성자를 호출 해 `ErrorCode`를 설정하고 있는가?
- 서비스 계층이나 도메인 계층에서 `BusinessException`을 상속하지 않은 예외를 던지지 않는가?
- 서비스 계층이나 도메인 계층에서 본인의 도메인이 아닌 다른 도메인의 예외를 던지지 않는가?
- 타인 소유 리소스 조회/수정/삭제 시 403 대신 404를 반환하는가?
- 해당 로직/메서드에서 발생할 수 있는 예외 중 `GlobalExceptionHandler`에서 핸들링되지 않는 예외가 있는가?
### 2.2. API
- Bean Validation을 사용하는 Request DTO 매개변수에 `@Valid`가 붙어 있는가?
- DTO 대신 Entity를 직접 노출하지 않는가?
### 2.3. DB
- N+1 문제가 생길 수 있는 연관 조회가 있는가?
### Etc.
- 메서드 시그니처/네이밍 변경 혹은 특정 필드명 변경 등 사이드 이펙트가 일어날 수 있는 상황에서 해당 요소를 참조/사용하는 코드 중 변경사항이 반영되지 않은 코드가 있는가? (+ 주석이 최신화 되었는가?)
- DB/외부 시스템에서 조회된 값을 애플리케이션 레이어에서 필터링/정렬 등 추가적으로 처리하는 코드가 있다면, 해당 코드에서 버그가 일어날 수 있는 부분이 있는가? 혹은 개선이 가능한 부분이 있는가?

