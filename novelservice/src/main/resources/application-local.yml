spring:
  datasource:
    url: 'jdbc:mysql://localhost:22306/novel_service?serverTimezone=Asia/Seoul&rewriteBatchedStatements=true'
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true